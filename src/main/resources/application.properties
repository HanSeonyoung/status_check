# ==== RTSP ====
RTSP_USERNAME=${RTSP_USERNAME}
RTSP_PASSWORD=${RTSP_PASSWORD}
RTSP_PORT=${RTSP_PORT:554}
RTSP_PATH=${RTSP_PATH}

# ==== READ DB (cctv_read; 172.30.29.236) ====
spring.datasource.read.driver-class-name=${READ_DB_DRIVER_CLASS_NAME}
spring.datasource.read.jdbc-url=${READ_DB_URL}
spring.datasource.read.username=${READ_DB_USERNAME}
spring.datasource.read.password=${READ_DB_PASSWORD}

# ==== METRICS DB (health_metrics; 172.30.29.237) ====
spring.datasource.metrics.driver-class-name=${METRICS_DB_DRIVER_CLASS_NAME}
spring.datasource.metrics.jdbc-url=${METRICS_DB_URL}
spring.datasource.metrics.username=${METRICS_DB_USERNAME}
spring.datasource.metrics.password=${METRICS_DB_PASSWORD}

# ==== App Configuration (????? ??) ====
app.environment-mode=${APP_ENVIRONMENT_MODE:DEV_ENV1}
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000,http://frontend.cccr4.com,https://frontend.cccr4.com

# ==== AWS Configuration (??? ????) ====
cloud.aws.region.static=${AWS_REGION:ap-northeast-2}
cloud.aws.stack.auto=${AWS_STACK_AUTO:false}

# ==== Logging ====
logging.level.com.hanseonyoung.statuscheck=${LOG_LEVEL:INFO}